<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.silex.tasktracker.persistence.mybatis.TimeRecordEntityMapper">

    <resultMap id="TimeRecordEntityMap" type="ru.silex.tasktracker.persistence.model.TimeRecordEntity">
        <id column="id" property="id"/>
        <result column="employee_id" property="employeeId"/>
        <result column="task_id" property="taskId"/>
        <result column="started_at" property="startedAt"/>
        <result column="finished_at" property="finishedAt"/>
        <result column="work_description" property="workDescription"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO time_records (employee_id, task_id, started_at, finished_at, work_description)
        VALUES (#{employeeId}, #{taskId}, #{startedAt}, #{finishedAt}, #{workDescription})
    </insert>

    <select id="selectByEmployeeAndStartedAtBetween" resultMap="TimeRecordEntityMap">
        SELECT id, employee_id, task_id, started_at, finished_at, work_description
        FROM time_records
        WHERE employee_id = #{employeeId}
          AND started_at &gt;= #{fromInclusive}
          AND started_at &lt;= #{toInclusive}
        ORDER BY started_at
    </select>
</mapper>
